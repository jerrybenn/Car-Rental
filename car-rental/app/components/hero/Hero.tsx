import React from 'react';
import Link from 'next/link';
import './hero.css'; // Importing the CSS file

const Hero = () => {
  return (
    <main className="hero-container">
        <div className="hero-text">tex</div>
        <div className="hero-image">imageree</div>
    </main>
  );
}

export default Hero;
